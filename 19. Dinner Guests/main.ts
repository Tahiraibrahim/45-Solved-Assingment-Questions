#! /usr/bin/env node

import{guest_list} from '../14. Guest list/app.js'
console.log(`n\ PRINTING NUMBER OF GUEST INVITED`)
console.log(`We have finally invited ${guest_list.length} from exercise 14 `);