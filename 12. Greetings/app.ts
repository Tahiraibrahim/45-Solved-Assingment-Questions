#! /usr/bin/env node

let members = ['tahira', 'fahmida', 'saima', 'faiza', 'kainat'];
for (var i = 0; i < members.length; i++) {
let message: string= 'March is my birthday month:';
    console.log(message + members[i]);
}
